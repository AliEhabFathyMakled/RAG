'use client'

import { useMemo, useState } from 'react'
import { Activity, ArrowUp, BookOpen, Check, ChevronDown, FileText, FlaskConical, Search, ShieldCheck, SlidersHorizontal, Sparkles, Upload, UserRound } from 'lucide-react'

const sampleDocs = [{ name: 'clinical_guidelines.pdf', meta: '1.2 MB · 42 pages', status: 'Indexed', color: 'bg-primary' }, { name: 'pharmacology_reference.pdf', meta: '3.8 MB · 118 pages', status: 'Indexed', color: 'bg-accent' }]
const exampleQuestion = 'What are the first-line treatments for community-acquired pneumonia in adults?'
const citations = [{ title: 'IDSA/ATS Consensus Guidelines on CAP', detail: 'Clinical Infectious Diseases · 2019', score: '0.94' }, { title: 'Diagnosis and Treatment of Adults with Community-Acquired Pneumonia', detail: 'American Family Physician · 2022', score: '0.88' }]

export default function Home() {
  const [question, setQuestion] = useState('')
  const [asked, setAsked] = useState(false)
  const [showDocs, setShowDocs] = useState(true)
  const [tab, setTab] = useState<'chat' | 'retrieval'>('chat')
  const ready = useMemo(() => question.trim().length > 0, [question])
  function submit() { if (ready) setAsked(true) }
  return <main className="min-h-screen bg-background">
    <header className="border-b bg-card"><div className="mx-auto flex max-w-[1400px] items-center justify-between px-5 py-4 lg:px-8"><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-lg bg-primary text-primary-foreground"><FlaskConical className="size-5" /></div><div><div className="font-semibold tracking-tight">Clinical RAG</div><div className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Knowledge Assistant</div></div></div><div className="flex items-center gap-5 text-sm text-muted-foreground"><div className="hidden items-center gap-2 md:flex"><span className="size-2 rounded-full bg-primary" />System operational</div><button className="flex items-center gap-2 rounded-md border px-3 py-2 text-foreground hover:bg-muted"><UserRound className="size-4" />Dr. Wilson<ChevronDown className="size-3" /></button></div></div></header>
    <div className="mx-auto grid max-w-[1400px] lg:grid-cols-[280px_1fr]">
      <aside className="hidden min-h-[calc(100vh-74px)] border-r bg-card p-5 lg:block"><div className="mb-7 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground">Workspace</p><h2 className="mt-1 font-semibold">Clinical Library</h2></div><button className="rounded-md p-2 text-muted-foreground hover:bg-muted"><SlidersHorizontal className="size-4" /></button></div><button className="mb-5 flex w-full items-center justify-center gap-2 rounded-md bg-primary px-3 py-2.5 text-sm font-medium text-primary-foreground hover:opacity-90"><Upload className="size-4" />Add documents</button><div className="mb-3 flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-[0.14em] text-muted-foreground">Sources <span className="rounded-full bg-muted px-1.5 py-0.5 text-[10px]">2</span></span><Search className="size-4 text-muted-foreground" /></div><div className="flex flex-col gap-2">{sampleDocs.map((doc) => <div key={doc.name} className="rounded-lg border bg-background p-3"><div className="flex items-start gap-3"><div className={`mt-0.5 flex size-8 shrink-0 items-center justify-center rounded ${doc.color} text-primary-foreground`}><FileText className="size-4" /></div><div className="min-w-0"><p className="truncate text-sm font-medium">{doc.name}</p><p className="mt-1 text-xs text-muted-foreground">{doc.meta}</p><p className="mt-2 flex items-center gap-1.5 text-xs text-primary"><Check className="size-3" />{doc.status}</p></div></div></div>)}</div><div className="mt-auto pt-10"><div className="rounded-lg border bg-muted/50 p-3"><div className="flex items-center gap-2 text-sm font-medium"><ShieldCheck className="size-4 text-primary" />Evidence mode</div><p className="mt-2 text-xs leading-relaxed text-muted-foreground">Answers are grounded in your indexed sources and include citations.</p></div></div></aside>
      <section className="min-w-0"><div className="border-b px-5 py-5 lg:px-10"><div className="mx-auto max-w-4xl"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.15em] text-primary"><Activity className="size-3.5" />Clinical workspace</div><div className="mt-2 flex items-end justify-between gap-4"><div><h1 className="text-2xl font-semibold tracking-tight lg:text-3xl">Ask your clinical library</h1><p className="mt-1 text-sm text-muted-foreground">Retrieve precise, evidence-grounded answers from your documents.</p></div><div className="hidden items-center gap-2 rounded-md border bg-card px-3 py-2 text-xs text-muted-foreground sm:flex"><span className="font-mono text-foreground">v1.4</span> · Hybrid retrieval</div></div></div></div>
        <div className="mx-auto max-w-4xl px-5 py-8 lg:px-10"><div className="flex border-b"><button onClick={() => setTab('chat')} className={`border-b-2 px-1 pb-3 text-sm font-medium ${tab === 'chat' ? 'border-primary text-primary' : 'border-transparent text-muted-foreground'}`}>Assistant</button><button onClick={() => setTab('retrieval')} className={`ml-6 border-b-2 px-1 pb-3 text-sm font-medium ${tab === 'retrieval' ? 'border-primary text-primary' : 'border-transparent text-muted-foreground'}`}>Retrieval details</button></div>{tab === 'retrieval' ? <div className="py-8"><div className="grid gap-4 sm:grid-cols-3">{[['Chunking','Section-aware'],['Retrieval','Dense + BM25'],['Reranking','Cross-encoder']].map(([a,b]) => <div key={a} className="rounded-lg border bg-card p-4"><p className="text-xs uppercase tracking-widest text-muted-foreground">{a}</p><p className="mt-2 font-medium">{b}</p></div>)}</div><div className="mt-6 rounded-lg border bg-card p-5"><h3 className="font-medium">Pipeline configuration</h3><div className="mt-4 flex flex-col gap-3 text-sm">{['PDF text extraction and normalization','Hybrid similarity search over indexed chunks','Cross-encoder reranking with confidence threshold','Citation-backed answer synthesis'].map((x) => <div key={x} className="flex items-center gap-3"><Check className="size-4 text-primary" />{x}</div>)}</div></div></div> : <><div className="flex min-h-[380px] flex-col justify-end py-8">{!asked ? <div className="flex flex-col items-center justify-center text-center"><div className="mb-5 flex size-14 items-center justify-center rounded-2xl border bg-card text-primary shadow-sm"><Sparkles className="size-6" /></div><h2 className="text-lg font-semibold">What would you like to investigate?</h2><p className="mt-2 max-w-md text-sm leading-relaxed text-muted-foreground">Ask a clinical question and I&apos;ll search your sources, synthesize the evidence, and show exactly where the answer came from.</p><button onClick={() => setQuestion(exampleQuestion)} className="mt-5 flex items-center gap-2 rounded-md border bg-card px-3 py-2 text-left text-xs text-muted-foreground hover:bg-muted"><BookOpen className="size-3.5 text-primary" />Try an example question</button></div> : <div className="flex flex-col gap-6"><div className="ml-auto max-w-[85%] rounded-2xl rounded-br-sm bg-primary px-4 py-3 text-sm leading-relaxed text-primary-foreground">{question}</div><div className="rounded-xl border bg-card p-5 shadow-sm"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-primary"><Sparkles className="size-3.5" />Evidence-grounded answer</div><p className="mt-4 text-sm leading-7">For otherwise healthy adults, recommended first-line outpatient treatment depends on comorbidities and local resistance patterns. Options commonly include amoxicillin or doxycycline; patients with comorbidities may require combination therapy or a respiratory fluoroquinolone. Severity assessment and local guidelines should guide the final choice.</p><div className="mt-5 border-t pt-4"><button onClick={() => setShowDocs(!showDocs)} className="flex w-full items-center justify-between text-left text-xs font-semibold uppercase tracking-widest text-muted-foreground">Sources <ChevronDown className={`size-4 transition-transform ${showDocs ? 'rotate-180' : ''}`} /></button>{showDocs && <div className="mt-3 flex flex-col gap-2">{citations.map((c, i) => <div key={c.title} className="flex items-center justify-between gap-3 rounded-md bg-muted/60 px-3 py-2.5"><div className="flex min-w-0 items-center gap-2"><span className="font-mono text-xs text-primary">[{i + 1}]</span><div className="min-w-0"><p className="truncate text-sm font-medium">{c.title}</p><p className="text-xs text-muted-foreground">{c.detail}</p></div></div><span className="shrink-0 font-mono text-xs text-primary">{c.score}</span></div>)}</div>}</div></div></div>}</div><div className="rounded-xl border bg-card p-2 shadow-sm"><textarea value={question} onChange={(e) => setQuestion(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey && !e.nativeEvent.isComposing && e.keyCode !== 229) { e.preventDefault(); submit() } }} placeholder="Ask a question about your clinical sources..." className="min-h-16 w-full resize-none bg-transparent px-3 py-2 text-sm outline-none placeholder:text-muted-foreground" /><div className="flex items-center justify-between px-2 pb-1"><span className="text-xs text-muted-foreground">Answers cite indexed documents</span><button onClick={submit} disabled={!ready} aria-label="Submit question" className="flex size-9 items-center justify-center rounded-lg bg-primary text-primary-foreground disabled:cursor-not-allowed disabled:opacity-40"><ArrowUp className="size-4" /></button></div></div><p className="mt-3 text-center text-[11px] text-muted-foreground">Clinical RAG can make mistakes. Verify important decisions against the original source.</p></>}</div></section></div></main>
}
